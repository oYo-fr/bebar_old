{
  "name": "bebar-sample",
  "description": "Sample bebar file",
  "scripts": {
    "go": "bebar -f ./*.bebar -w .",
    "dev": "nodemon --config nodemon.json -e mustache,js,bebar,yaml,yml,json --exec bebar -f ./*.bebar -w ."
  },
  "author": "Yoann D.",
  "devDependencies": {
    "bebar": "*",
    "nodemon": "^2.0.6"
  }
}
