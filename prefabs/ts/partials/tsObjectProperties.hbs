{{#each definition.properties}}
{{>tsComment description}}
  {{#if properties}}
    {{toCamelCase @key}}: {
    {{>tsObjectProperties object=.}}
    }
  {{else}}
    {{toCamelCase @key}}: {{#if type}} {{type}} {{else}} object {{/if}}
  {{/if}}
{{/each}}